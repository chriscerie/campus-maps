{
  "name": "campus-map",
  "private": true,
  "scripts": {
    "test": "yarn --cwd client test",
    "test-report": "yarn --cwd client test-report",
    "prettier": "yarn --cwd client prettier",
    "lint": "yarn --cwd client lint"
  }
}
